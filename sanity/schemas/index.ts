import heroSchema from "./hero-schema";
import aboutSchema from "./about-schema";
import skillsSchema from "./skills-schema";

const schemas = [heroSchema, aboutSchema, skillsSchema];

export default schemas;
